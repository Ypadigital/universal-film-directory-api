// Update if needed
export interface IProject {
  name: string;
  description: string;
  tokenId: number;
  pricePerFraction: number;
  totalSupply: number;
  traits: string[];
  curator: string;
  image: string;
}
